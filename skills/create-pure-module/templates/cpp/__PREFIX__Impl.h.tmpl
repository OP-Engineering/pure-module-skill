#pragma once

// Generated by react-native-codegen from src/Native__PREFIX__.ts at build
// time (RN's Android/iOS codegen tasks write it into this library's own
// android/generated and ios/generated dirs) — do not hand-write it.
#include <__PREFIX__SpecJSI.h>

#include <memory>

namespace facebook::react {

// The thin entry point: the only codegen'd method is a blocking
// install(rt) call that hands the JSI Runtime + this TurboModule's
// CallInvoker to hand-written C++ — see __PREFIX__Module.cpp. Everything
// else is plain JSI HostObjects installed onto a global proxy, not more
// codegen'd methods on this class.
class __PREFIX__Impl : public Native__PREFIX__CxxSpec<__PREFIX__Impl> {
public:
  __PREFIX__Impl(std::shared_ptr<CallInvoker> jsInvoker);

  bool install(jsi::Runtime &rt);
};

} // namespace facebook::react
